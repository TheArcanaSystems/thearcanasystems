# tenfoldmarc Creator OS — Dashboard

Content dashboard for the `@tenfoldmarc` creator business. Lives in this
`dashboard/` subfolder, not the repo root, because the repo root is the
`TheArcanaSystems` GitHub Pages profile site (`index.html` + `CNAME`) — keeping
the Next.js app in its own folder avoids clobbering that static site.

See `AGENTS.md` in this folder for Next.js 16 specific agent warnings (it ships
breaking changes vs. older training data; check `node_modules/next/dist/docs/`
before assuming API shapes).

## Stack

- **Next.js 16** (App Router, Turbopack by default, TypeScript, `src/` dir)
- **Tailwind CSS v4** (CSS-variable theme via `@theme inline` in `globals.css`, no `tailwind.config.*`)
- **shadcn/ui** ("new-york" style, Radix primitives) — see note below
- **next-themes** for dark/light mode (class strategy, default dark)
- **recharts** for the Analytics chart, wrapped in shadcn's `chart.tsx`
- **lucide-react** for icons

## Decisions worth knowing about

- **shadcn/ui components are hand-written, not CLI-generated.** This sandbox's
  network policy blocks `ui.shadcn.com`, so `npx shadcn@latest add ...` cannot
  reach the registry. Every file in `src/components/ui/` was written by hand to
  match the standard shadcn "new-york" conventions (`data-slot` attributes,
  `cva` variants, `cn()` from `@/lib/utils`, aliases from `components.json`).
  In an environment with network access, the shadcn CLI should still recognize
  this setup and can be used normally going forward — these files just won't
  have been generated by it.
- **lucide-react ships no brand icons.** `Instagram`/`Youtube`/`Twitter` etc.
  were removed upstream for trademark reasons. `src/lib/platform.ts` maps each
  `Platform` to a neutral lucide icon (`Camera`, `Music2`, `Clapperboard`, `X`)
  tinted with the brand's color via Tailwind text classes — not a literal logo.
- **Dark mode is the default**, set via `next-themes` (`defaultTheme="dark"`,
  `enableSystem={false}`) on `<ThemeProvider>` in `src/app/layout.tsx`. Light
  mode exists and is reachable via the toggle in the sidebar/topbar, but every
  page was designed against the dark palette first.
- **Terracotta accent**: `--primary` is `#b85b38` (light) / `#c1693f` (dark),
  defined as CSS variables in `src/app/globals.css` alongside the rest of the
  shadcn token set (`background`, `card`, `accent`, `chart-1..5`,
  `sidebar*`, etc.). Change the accent by editing those variables only — don't
  hardcode the hex anywhere else.
- **Route structure**: `src/app/(dashboard)/layout.tsx` wraps all six pages
  with the shared `AppSidebar` + `Topbar` shell. `src/app/page.tsx` just
  redirects to `/hook-vault`, which is treated as the default/home page.
- **Sidebar identity**: the creator handle `@tenfoldmarc` is pinned at the top
  of the sidebar (`src/components/dashboard/creator-profile.tsx`), above the
  six-item nav defined in `src/config/nav.ts`. Mobile uses the same nav inside
  a shadcn `Sheet` triggered from the topbar hamburger button.
- **All data is mocked.** Every page reads from a typed fixture in
  `src/data/*.ts` (types in `src/lib/types.ts`) — there is no backend yet. The
  data files are the intended integration seam:
  - `data/hooks.ts` → Hook Vault (each `Hook` carries a `hookType`, labeled
    via `lib/hook-type.ts`, for the type-based filter)
  - `data/analytics.ts` → Analytics (daily metrics + weekly "heaters")
  - `data/competitors.ts` → Competitor Tracker (tracked creators + their reels)
  - `data/scheduler.ts` → Scheduler queue (the page also holds ephemeral
    client-side `useState` on top of this seed so "New post" feels real
    without a backend)
  - `data/calendar.ts` → Content Calendar (entries tagged `/script` vs `manual`)
  - `data/trending.ts` → What's Trending (12 named sources + tagged items)

  Wiring up real sources later means replacing these modules with actual
  fetches (Instagram Graph API, a scraper job, your scheduling provider, an
  RSS/news aggregation job, and whatever produces `/script` output) — the page
  components themselves shouldn't need to change shape, just their data source.

## Commands

```bash
cd dashboard
npm run dev      # start dev server (Turbopack)
npm run build    # production build
npm run lint     # eslint (flat config, react-hooks rules included)
```

## Page notes

- **Hook Vault** — card grid of saved hooks with hook text, fill-in-the-blank
  template, platform/niche/angle tags, and status (unused/scheduled/used).
  Searchable/filterable by free-text (hook text + creator), niche, hook type
  (`HookType` in `lib/types.ts`: callout, command, listicle, proof, pov,
  contrarian, secret, documentary), and a min-views threshold. "Use this"
  marks an unused hook `scheduled` and flips the button to "Sent to /script"
  — local state only for now, since there's no real `/script` pipeline yet.
- **Analytics** — stat cards (views/saves/follows/engagement) with week-over-week
  delta, a 14-day area chart, and a "heaters of the week" table ranked by a
  mocked heat score.
- **Competitor Tracker** — one card per tracked creator showing their top reels
  this week and a "scraped Xh ago" badge.
- **Scheduler** — queue table + a dialog to draft a post: pick a hook, hit
  Autogenerate to fill a caption from that hook's template/angle, choose
  platforms, schedule. New posts are appended to local state immediately.
- **Content Calendar** — custom month-grid (no date-picker library) marking
  each entry's source as `/script` (terminal icon) or `manual` (user icon).
- **What's Trending** — items from 12 named sources sorted by hook potential
  (high/medium/low), each with a "Draft hook" quick action.
